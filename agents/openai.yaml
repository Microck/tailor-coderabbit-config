interface:
  display_name: "Tailor CodeRabbit Config"
  short_description: "Build and calibrate repository-specific reviews"
  default_prompt: "Use $tailor-coderabbit-config to create or tune this repository's .coderabbit.yaml from codebase evidence and real review feedback."
